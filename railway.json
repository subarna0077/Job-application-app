{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --omit=dev"
  },
  "deploy": {
    "startCommand": "npx json-server-auth db.json --port $PORT --host 0.0.0.0",
    "restartPolicyType": "ON_FAILURE"
  }
}